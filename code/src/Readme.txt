1.Install Dependencies:

pip install streamlit gradio numpy pandas scikit-learn

2. Running the App:

To run this interactive application:

Save the script as {filename}.py or {filename}.ipynb.

Open a terminal and run the following command to start the Streamlit app:

streamlit run app.py